/* IMPORT */
import { forIndex } from '../oby.js';
/* MAIN */
const ForIndex = ({ values, fallback, children }) => {
    return forIndex(values, children, fallback);
};
/* EXPORT */
export default ForIndex;
