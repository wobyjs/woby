import type { SuspenseData } from '../types';
export declare class SuspenseManager {
    private suspenses;
    change: (suspense: SuspenseData, nr: number) => void;
    suspend: () => void;
    unsuspend: () => void;
}
//# sourceMappingURL=suspense.manager.d.ts.map