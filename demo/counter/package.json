{
    "private": true,
    "name": "counter",
    "type": "module",
    "scripts": {
        "dev": "vite --force --mode dev --host",
        "prod": "vite --force",
        "bump": "pnpm version patch --no-git-tag-version",
        "build": "vite build && pnpm bump"
    },
    "dependencies": {
        "vite": "^4.4.11",
        "woby": "workspace:*"
    },
    "devDependencies": {},
    "version": "0.0.2"
}