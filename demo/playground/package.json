{
    "private": true,
    "name": "playground",
    "type": "module",
    "scripts": {
        "dev": "vite --force --mode dev --host",
        "prod": "vite --force",
        "bump": "pnpm version patch --no-git-tag-version",
        "build": "vite build && pnpm bump",
        "test": "tsx --tsconfig tsconfig.json src/test.tsx"
    },
    "dependencies": {
        "vite": "^8.1.5",
        "woby": "workspace:../.."
    },
    "devDependencies": {
        "@types/node": "^24.10.13",
        "tsx": "^4.19.2"
    },
    "version": "0.0.53"
}
