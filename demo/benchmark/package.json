{
    "private": true,
    "name": "benchmark",
    "type": "module",
    "scripts": {
        "dev": "vite --force --mode dev --host",
        "prod": "vite --force",
        "build": "vite build",
        "test": "playwright test"
    },
    "dependencies": {
        "vite": "^4.4.11",
        "woby": "workspace:*"
    }
}