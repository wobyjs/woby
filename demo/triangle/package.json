{
    "private": true,
    "name": "triangle",
    "type": "module",
    "scripts": {
        "dev": "vite --force --mode dev --host",
        "prod": "vite --force",
        "build": "vite build",
        "test:e2e": "playwright test triangle.spec.js",
        "test:e2e:ui": "playwright test triangle.spec.js --ui"
    },
    "dependencies": {
        "vite": "^4.4.11",
        "woby": "workspace:*"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.1"
    }
}