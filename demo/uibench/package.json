{
    "private": true,
    "name": "uibench",
    "type": "module",
    "scripts": {
        "dev": "vite --force --mode dev --host",
        "prod": "vite --force",
        "build": "vite build"
    },
    "dependencies": {
        "vite": "^4.5.14",
        "woby": "workspace:*"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.1",
        "@types/node": "^24.10.13"
    }
}